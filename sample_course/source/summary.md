A worked example of every feature the pluto course format supports: all four
step types, in-course links, marks, config variables and abbreviations. Each
step documents the feature it demonstrates, so the course reads as a tutorial
for the format itself.
