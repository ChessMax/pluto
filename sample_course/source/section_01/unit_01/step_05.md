---
id:
type: free_answer
manual_scoring: false
is_attachments_enabled: false
is_html_enabled: true
---

A `free_answer` step takes prose. There is nothing to check automatically, so
the interesting keys are about how it is graded and what the student may send:
`manual_scoring` puts it in an instructor's review queue, and
`is_attachments_enabled` lets them attach a file.

In your own words: why does ordering come from the file name rather than from a
`position:` field in the front matter?
