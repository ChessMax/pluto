This course is not meant to teach anything but `pluto` itself. It exists in the
repository as a worked example: a course source tree that exercises every
documented feature, so a change to the format has somewhere to fail visibly.

Read it alongside [COURSE_SYNTAX.md](../../COURSE_SYNTAX.md), which is the
reference this course demonstrates.
